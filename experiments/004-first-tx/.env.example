# Copy this file to `.env` (which is gitignored) and fill in the values.
#
# PRIVATE_KEY: the throwaway testnet key from Lesson 3.
# TO_ADDRESS:  any other address — could be the zero address (burn),
#              a second key you generate, or your real wallet's address.
# AMOUNT_ETH:  optional, defaults to 0.0001.
#
# REMINDER: testnet only. Never put a real-money key here.

PRIVATE_KEY=0x...
TO_ADDRESS=0x...
AMOUNT_ETH=0.0001
